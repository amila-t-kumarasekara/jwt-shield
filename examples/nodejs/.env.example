# JWT Configuration
# Generate these keys using a secure method and store them safely
# Example of generating keys:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# The encryption key must be a 32-byte key encoded in base64
JWT_ENCRYPTION_KEY=

# The signing key can be any secure random string encoded in base64
JWT_SIGNING_KEY=

# Optional: Override the default expiration time (default: 1h)
JWT_EXPIRES_IN= 